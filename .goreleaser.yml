builds:
  - binary: flow
    goos:
      - darwin
      - linux
    goarch:
      - amd64
      - arm64
    env:
      - CGO_ENABLED=0

release:
  mode: keep-existing
  prerelease: auto

universal_binaries:
  - replace: true

brews:
  - name: flow
    homepage: https://github.com/Edens-Angel/flow-cli
    repository:
      owner: edens-angel
      name: flow-cli
    commit_author:
      name: edens-angel

checksum:
  name_template: "checksums.txt"
